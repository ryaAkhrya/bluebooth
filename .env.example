# Optional. Leave both Supabase values blank to run Bluebooth in local room mode.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Used for share links when no browser origin is available.
NEXT_PUBLIC_APP_URL=http://localhost:3000
